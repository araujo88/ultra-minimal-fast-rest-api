# Test-only dependencies (development, not shipped or used by the C server).
# Lower bounds are set to patched releases to clear known CVEs; pip resolves the
# newest compatible versions at or above these floors. Transitive packages that
# had advisories (urllib3, certifi, idna) are floored explicitly below.
pytest>=9.1.1
requests>=2.33.0
faker>=40.37.0

# Security floors for requests' transitive dependencies.
urllib3>=2.7.0
certifi>=2024.7.4
idna>=3.15
